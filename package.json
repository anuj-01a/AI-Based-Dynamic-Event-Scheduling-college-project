{
  "name": "festival-scheduler-mern",
  "private": true,
  "version": "1.0.0",
  "description": "AI-Based Dynamic Event Scheduling and Conflict Resolution System for Large-Scale Festivals",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n api,web \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "seed": "node server/src/seeds/seed.js",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
